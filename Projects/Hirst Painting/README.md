# 🎨 Hirst Painting

Creates a Damien Hirst–inspired dot painting using Python’s built-in `turtle` graphics. The script draws a 10×10 grid of colorful dots from a predefined palette.

## 👀 Preview
When you run the program, a window opens and draws the dot grid. Click the window to close it.

## ✅ Requirements
- 🐍 Python 3.x
- 📦 Standard library only (`turtle`, `random`)

## ▶️ How to Run
From the project folder:

1. 🧾 Open a terminal.
2. ▶️ Run:
   - 🪟 Windows (PowerShell):
     - `python main.py`

## 📝 Notes
- 🎨 The palette is defined in `main.py` as a list of RGB tuples.
- 🌈 The canvas uses `t.colormode(255)` for RGB colors.

## 📂 File Overview
- 🐍 `main.py` — draws the dot painting.

## 🙌 Credit
Inspired by the “Hirst Painting” project from beginner Python courses.
